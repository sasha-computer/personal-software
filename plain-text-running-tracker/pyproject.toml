[project]
name = "plain-text-running-tracker"
version = "0.1.0"
description = "Parse Apple Health exports and Garmin FIT files into a simple markdown running log"
requires-python = ">=3.13"
dependencies = [
    "fitdecode>=0.11.0",
]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]
